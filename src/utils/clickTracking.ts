
/**
 * Track page view
 * This function is currently a placeholder and doesn't actually track anything
 * It's here to fix TypeScript errors in BookingContainer
 */
export const trackPageView = (params: any) => {
  console.log('Page view tracked:', params);
  // Actual tracking implementation would go here
};
